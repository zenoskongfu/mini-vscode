{
  "name": "word-count",
  "displayName": "Word Count",
  "description": "Shows a (mock) word count for the active document.",
  "publisher": "mini-vscode",
  "version": "1.0.0",
  "main": "./extension.js",
  "activationEvents": ["onCommand:word-count.count"],
  "contributes": {
    "commands": [
      { "command": "word-count.count", "title": "Count Words", "category": "Word Count" }
    ]
  }
}
